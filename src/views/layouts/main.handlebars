<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}} - Admin Panel</title>
    <link rel="stylesheet" href="/css/main.css" />
    {{{section "styles"}}}
  </head>
  <body>
    <header>

      <nav class="main-nav">
        <div class="nav-brand">
          <a href="/">Poke-Mart Admin</a>
        </div>
        <div class="nav-links">
          {{#if user.isAdmin}}
            <a href="/items">Items</a>
            <a href="/users">Users</a>
          {{/if}}
          {{#if user}}
            <a href="/logout">Logout</a>
          {{/if}}
        </div>
      </nav>
    </header>

    <main>
      {{{body}}}
    </main>

    <footer>
      <div class="footer-content">
        <p>&copy; 2024 Poke-Mart Admin Panel</p>
      </div>
    </footer>

    {{{section "scripts"}}}
  </body>
</html>